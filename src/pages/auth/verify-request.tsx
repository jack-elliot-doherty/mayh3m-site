import Layout from "../../components/layout";

const VerifyRequest = ({}) => {
  return <div className="w-3/4 max-w-md text-center sm:w-1/2">test</div>;
};

VerifyRequest.getLayout = (page) => <Layout>{page}</Layout>;

export default VerifyRequest;
